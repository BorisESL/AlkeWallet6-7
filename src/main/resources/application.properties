# Nombre de la aplicación y puerto del servidor
spring.application.name=AlkeWallet6
server.port=8080

# Configuración de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/AlkeWallet6?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=conycata2019
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Propiedades de JPA (Java Persistence API)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Inicialización de la base de datos (comentada si no se usa)
# spring.datasource.initialization-mode=always
